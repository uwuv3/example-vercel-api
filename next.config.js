/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["cdn.discordapp.com", "i.ibb.co","i.imgur.com"],
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
